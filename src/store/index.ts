import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

function createStore () {
  return new Vuex.Store({
    modules: {
    },
    state: {
    },
    mutations: {
    },
    actions: {
    }
  });
}

export default createStore;
